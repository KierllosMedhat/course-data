// ASSIGNMENT — API Client with Tests (TypeScript Tooling)

// TODO: Create a tsconfig.json file in this directory to configure the TypeScript compiler.
// Set "strict": true, "target": "es2022", and "outDir": "./dist"

// TODO: Define an interface for the expected API response (e.g., Post { id: number, title: string, body: string })
// interface Post { ... }

// TODO: Create a class 'ApiClient'
// class ApiClient {
//   private baseUrl: string;
//   constructor(baseUrl: string) { this.baseUrl = baseUrl; }
//
//   // TODO: Add an async method 'fetchData(endpoint: string)' that uses the built-in fetch API
//   // It should return a Promise of the generic type T.
//   // async fetchData<T>(endpoint: string): Promise<T> { ... }
// }

// TODO: Write unit tests for your ApiClient (conceptually, in comments if you don't have Jest set up)
// describe('ApiClient', () => {
//     it('should fetch data successfully', async () => {
//         // ... test logic
//     });
//     it('should handle network errors', async () => {
//         // ... test logic
//     });
// });
