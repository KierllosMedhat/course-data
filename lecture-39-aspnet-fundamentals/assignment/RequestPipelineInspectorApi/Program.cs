// ASSIGNMENT — Request Pipeline Inspector API
// Modify the generated Program.cs

/*
using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;

var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

// TODO: 1. Add a custom inline middleware using app.Use()
// This middleware should measure how long a request takes to process.
// Hint: Use System.Diagnostics.Stopwatch

// TODO: 2. Add a custom inline middleware using app.Use()
// This middleware should check if a specific header (e.g., "X-Secret-Key") is present.
// If not, it should short-circuit the pipeline and return a 401 Unauthorized status.

// TODO: 3. Create a simple endpoint using app.MapGet("/api/data", ...)
// It should return a JSON object with some dummy data.

app.Run();
*/
