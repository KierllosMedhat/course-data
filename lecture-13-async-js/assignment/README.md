# TaskFlow Project Part 5

For this assignment, follow the instructions in the Lecture 13 Markdown file.

1. Continue using your folder from Lecture 12.
2. In your `taskModel.js`, create a new function called `syncTasksWithServer()`.
3. Use `fetch` (with `async/await` and `try/catch`) inside of it to hit a dummy endpoint like `https://jsonplaceholder.typicode.com/posts`
4. Call this function whenever tasks are modified.

Happy coding!
