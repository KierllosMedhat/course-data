# Lab 1: Unit Testing

1. Run `dotnet new xunit -n ShopAPI.Tests`.
2. Add a reference to your `ShopAPI` project.
3. Install `Moq`.
4. Create a test class for your `GetProductsQueryHandler`.
5. Mock the `AppDbContext` or `IRepository`.
6. Assert that the handler returns the mocked data!
