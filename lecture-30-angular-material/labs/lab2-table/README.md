# Lab 2: Data Table

1. Generate `user-table` component.
2. In the component class, define `displayedColumns = ['id', 'name', 'email']`.
3. Create some mock users and initialize `dataSource = new MatTableDataSource(users)`.
4. Build out the `<table mat-table>` in the HTML template. Use `<ng-container>` for each column definition.
5. Add `<mat-paginator>` below the table!
