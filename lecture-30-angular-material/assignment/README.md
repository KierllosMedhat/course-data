# ShopAngular Project Part 8

For this assignment, follow the instructions in the Lecture 30 Markdown file.

1. Add Angular Material to your ShopAngular project.
2. Use `<mat-toolbar>` for navigation.
3. Use `<mat-card>` for products.
4. Use `<mat-form-field>` for the checkout form.
5. Use `MatSnackBar` for a success message on checkout.

Happy coding!
