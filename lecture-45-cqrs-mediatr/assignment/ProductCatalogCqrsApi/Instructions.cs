// ASSIGNMENT — Product Catalog CQRS API

/*
// TODO 1: Add MediatR package
// dotnet add package MediatR

// TODO 2: Configure MediatR in Program.cs
// builder.Services.AddMediatR(cfg => cfg.RegisterServicesFromAssembly(typeof(Program).Assembly));

// TODO 3: Implement CQRS for 'Product' Entity
// 1. Create a `CreateProductCommand` and `CreateProductHandler`.
//    The handler should add the product to a mock list and return the new ID.
// 2. Create a `GetAllProductsQuery` and `GetAllProductsHandler`.
//    The handler should return the mock list of products.

// TODO 4: Use MediatR in the Controller
// Inject IMediator into your ProductsController.
// Use _mediator.Send(...) in your GET and POST endpoints.

// TODO 5 (Bonus): Output Caching
// In Program.cs: builder.Services.AddOutputCache(); and app.UseOutputCache();
// Decorate your GET endpoint with [OutputCache(Duration = 10)]
// Test hitting the endpoint multiple times to see the cached response.
*/
