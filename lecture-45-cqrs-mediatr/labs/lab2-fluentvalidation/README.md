# Lab 2: FluentValidation

1. Install `FluentValidation`.
2. Create a validator class inheriting from `AbstractValidator<CreateProductCommand>`.
3. Add rules for `Name` and `Price`.
4. Validate the command manually in your Controller or Handler.
