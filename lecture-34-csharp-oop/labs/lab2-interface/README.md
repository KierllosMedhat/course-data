# Lab 2: Interface

1. Define `public interface IRepository<T>`.
2. Add signatures: `void Add(T item);` and `List<T> GetAll();`.
3. Create `InMemoryRepository<T>` that implements the interface.
4. Use a private `List<T>` to store the items!
