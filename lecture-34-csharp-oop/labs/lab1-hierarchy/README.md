# Lab 1: Class Hierarchy

1. Define `public abstract class Person`.
2. Give it an `abstract void GetDescription();` method.
3. Define `public class Student : Person`.
4. Override the `GetDescription` method to print out the student's details!
