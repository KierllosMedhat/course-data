# FinanceTracker Project Part 4

For this assignment, follow the instructions in the Lecture 36 Markdown file.

1. Create a `StorageService` for your FinanceTracker.
2. Use `System.Text.Json` to save and load your transactions to a file.
3. Make sure to use `async/await` and `File.ReadAllTextAsync` / `File.WriteAllTextAsync`.

Happy coding!
