# Lab 2: JSON Serialization

1. Create a `User` record with `Name` and `Email` properties.
2. Initialize a `new User`.
3. Use `JsonSerializer.Serialize` to turn it into a string.
4. Try deserializing it back into an object using `JsonSerializer.Deserialize<User>()`.
