# Lab 1: Console Calculator

1. Run `dotnet new console` in this folder.
2. Open `Program.cs`.
3. Ask the user for two numbers using `Console.ReadLine()` and `double.Parse()`.
4. Ask for an operator (`+`, `-`, `*`, `/`).
5. Use a switch expression to perform the calculation!
