# Lab 2: Number Analysis

1. Run `dotnet new console` in this folder.
2. Ask the user for an integer using `int.Parse()`.
3. Write a `for` loop that calculates the factorial of the given number.
4. Print the result!
