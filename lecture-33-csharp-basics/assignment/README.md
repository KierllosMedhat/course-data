# FinanceTracker Project Part 1

For this assignment, follow the instructions in the Lecture 33 Markdown file.

1. Run `dotnet new console -n FinanceTracker`.
2. Define `public class Transaction(decimal Amount, string Description, string Category)`.
3. In `Program.cs`, create a loop to prompt the user to add transactions to a `List<Transaction>`.
4. When done, print all transactions and the total balance.

Happy coding!
