// ASSIGNMENT — Secure Task API (Authentication)

/*
// TODO 1: Add the JWT Bearer package to the project
// dotnet add package Microsoft.AspNetCore.Authentication.JwtBearer

// TODO 2: Configure Authentication in Program.cs
// Add AddAuthentication() and AddJwtBearer() with TokenValidationParameters.
// Ensure app.UseAuthentication() and app.UseAuthorization() are added to the pipeline.

// TODO 3: Create an AuthController
// Create an endpoint POST api/auth/login.
// It should accept a LoginDto (Username, Password).
// If credentials are valid (hardcode a check for "admin"/"password123"), 
// generate and return a JWT string using JwtSecurityTokenHandler.

// TODO 4: Secure the TasksController
// Create a TasksController with some dummy GET endpoints.
// Decorate the controller with [Authorize].
// Test accessing it without a token (should return 401 Unauthorized).
// Test accessing it with the token generated from the login endpoint.
*/
