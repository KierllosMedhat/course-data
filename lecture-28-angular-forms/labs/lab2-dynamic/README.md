# Lab 2: Dynamic Survey Builder

1. Create a form with a `questions` `FormArray`.
2. Add a method `addQuestion()` that pushes a new `FormControl` into the array.
3. In the template, iterate over `questions.controls` and render an input for each one!
