# Lab 1: Login Form Both Ways

1. Create a `TemplateDrivenLoginComponent`. Use `[(ngModel)]` and HTML attributes.
2. Create a `ReactiveLoginComponent`. Use `FormBuilder` and `Validators`.
3. Render both side-by-side in your app component and compare!
