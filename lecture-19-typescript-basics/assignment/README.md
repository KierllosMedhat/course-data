# DataForge Project Part 1

For this assignment, follow the instructions in the Lecture 19 Markdown file.

1. Setup a TypeScript project using `npm init -y` and `npx tsc --init`.
2. Ensure `strict` mode is enabled.
3. Build the `User`, `Dataset`, and `config` models.

Happy coding!
