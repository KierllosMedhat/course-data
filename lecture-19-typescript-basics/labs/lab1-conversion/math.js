// TODO: Rename this file to math.ts
// TODO: Add type annotations for a and b (they should be numbers)
// TODO: Add a return type annotation
function add(a, b) {
    return a + b;
}

// TODO: Add type annotations for radius
function calculateArea(radius) {
    return Math.PI * radius * radius;
}

// TODO: Run `npx tsc math.ts` in the terminal to compile it!
console.log(add(5, 10));
console.log(calculateArea(4));
