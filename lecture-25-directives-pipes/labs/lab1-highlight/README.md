# Lab 1: Highlight Directive

1. Generate your directive: `ng g d highlight`.
2. Add the `host` property to your directive decorator to bind `(mouseenter)` and `(mouseleave)`.
3. Add a signal input for the color.
4. Apply the directive to a `<p>` tag in your app template.
