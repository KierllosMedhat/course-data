# Lab 1: Generate an Angular v21 Project

1. Run `ng new angular-basics`.
2. Select your stylesheet format (CSS).
3. Select 'n' for Server-Side Rendering (SSR).
4. `cd angular-basics`
5. Run `ng serve` and visit `http://localhost:4200`.
