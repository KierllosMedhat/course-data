# ShopAngular Project Part 1

For this assignment, follow the instructions in the Lecture 23 Markdown file.

1. Generate your `shop-angular` project.
2. Build your `ProductListComponent`.
3. Experiment with the `@for` and `@if` control flow blocks.
4. Try using the new `resource()` API to fetch products!

Happy coding!
