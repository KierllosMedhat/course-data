import { describe, it, expect } from 'vitest';
import { UrlParser } from './url-parser';

describe('UrlParser.parse()', () => {
  // TODO: Write a test that parses a valid HTTP URL:
  // e.g. "http://example.com/" -> should return { protocol: "http", domain: "example.com", path: "/" }

  // TODO: Write a test that parses a valid HTTPS URL:
  // e.g. "https://my-app.io/dashboard"

  // TODO: Write a test that checks that passing an invalid URL throws an Error:
  // e.g. "not-a-url" -> expect(() => UrlParser.parse("...")).toThrow("Invalid URL format")

  // TODO: Write a test that parses a URL with query parameters:
  // e.g. "https://search.com/find?q=typescript&lang=en" -> should parse query parameters as record
});
