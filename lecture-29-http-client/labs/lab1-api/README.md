# Lab 1: Typed API Service

1. Generate your service: `ng g s product`.
2. Provide `HttpClient` via `app.config.ts`.
3. Create a `products` property using `rxResource()` to fetch from `https://fakestoreapi.com/products`.
4. Render the loading and error states in a component!
