# Lab 1: Unit Tests

1. Open `app.component.spec.ts`.
2. Ensure you have Vitest installed (or use standard Angular CLI testing).
3. Write a test that changes an input and asserts the DOM updates after `fixture.detectChanges()`.
