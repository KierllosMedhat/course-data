# Lab 1: Global Exception Handler

1. Create a `GlobalExceptionHandler` class.
2. Implement the `IExceptionHandler` interface.
3. Use the `ILogger` to log the error.
4. Return a `ProblemDetails` response.
5. Register it in `Program.cs` and test it!
