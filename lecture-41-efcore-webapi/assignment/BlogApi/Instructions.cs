// ASSIGNMENT — Blog API (EF Core Integration)

/*
// TODO 1: Create Entity Classes (Models)
// Create 'Post' (Id, Title, Content, AuthorId) and 'Author' (Id, Name, List<Post>) classes in a Models folder.

// TODO 2: Create the DbContext
// Create 'BlogDbContext' inheriting from DbContext. Add DbSets for Posts and Authors.

// TODO 3: Configure Program.cs
// Register the BlogDbContext with the Dependency Injection container using an in-memory database or SQLite.
// builder.Services.AddDbContext<BlogDbContext>(options => options.UseInMemoryDatabase("BlogDb"));

// TODO 4: Create PostsController
// Generate or manually create an API Controller with full CRUD operations for Posts.
// Inject the BlogDbContext into the controller.

// TODO 5 (Bonus): Add a DTO (Data Transfer Object)
// Instead of returning the raw Post entity (which might cause object cycle issues with Author), 
// return a PostDto containing only the necessary fields.
*/
