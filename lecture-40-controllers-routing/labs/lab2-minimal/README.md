# Lab 2: Minimal APIs

1. Open `Program.cs`.
2. Define a `Category` class.
3. Map endpoints for Categories using `app.MapGet()`, `app.MapPost()`, `app.MapPut()`, and `app.MapDelete()`.
4. Test them using Swagger!
