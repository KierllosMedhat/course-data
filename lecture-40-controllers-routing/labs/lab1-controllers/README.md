# Lab 1: CRUD Controller

1. Add a `Controllers` folder to your project.
2. Create `ProductsController.cs`.
3. Inherit from `ControllerBase`.
4. Add `[ApiController]` and `[Route("api/[controller]")]`.
5. Write `[HttpGet]`, `[HttpPost]`, `[HttpPut("{id}")]`, and `[HttpDelete("{id}")]` methods.
