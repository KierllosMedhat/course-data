// TODO: Define the strict TypeScript interface for a Post
// It should match the JSONPlaceholder structure: id, userId, title, body
export interface Post {
  id: number;
  userId: number;
  title: string;
  body: string;
}
