import { Routes } from '@angular/router';

// TODO: Define the routes for the ShopAngular routing assignment:
// - '/' redirects to '/products' (pathMatch: 'full')
// - '/products' maps to ProductListComponent
// - '/products/:id' maps to ProductDetailComponent
// - '/cart' maps to CartComponent (Bonus: can be lazy-loaded using loadComponent)
// - '**' maps to NotFoundComponent (wildcard route)

export const routes: Routes = [
  // Define routes here
];
