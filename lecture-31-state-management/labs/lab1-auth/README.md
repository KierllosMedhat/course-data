# Lab 1: Auth State Service

1. Create an `AuthStore` service.
2. Add private signals for `user`, `loading`, and `error`.
3. Add a public `computed()` signal for `isAuthenticated`.
4. Create a `login()` method that updates these signals.
