# Lab 2: Derived Selectors

1. Create a `ProductStore` service.
2. Add a signal array of products and a string signal for `filterText`.
3. Build a `computed()` signal named `filteredProducts` that returns the filtered array based on `filterText`.
4. Use `filteredProducts()` in your component's template!
