# ShopAngular Project Part 9

For this assignment, follow the instructions in the Lecture 31 Markdown file.

1. Refactor `CartService` to use a private writable Signal instead of a `BehaviorSubject`.
2. Ensure you use immutable updates (`.update()`).
3. Build `computed()` selectors for your cart totals.

Happy coding!
