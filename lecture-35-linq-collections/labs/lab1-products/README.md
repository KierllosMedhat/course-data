# Lab 1: Query Products

1. Define a `Product` class.
2. Initialize a `List<Product>` with 10 dummy products.
3. Use `.Where()`, `.OrderBy()`, and `.Select()` to query the list.
4. Iterate over the results and print them!
