# Lab 2: Frozen Config

1. Create a `Dictionary<string, decimal>`.
2. Populate it with 3 state tax rates.
3. Call `.ToFrozenDictionary()` on it.
4. Use `.TryGetValue()` to look up a state's tax rate.
