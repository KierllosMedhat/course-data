# ShopAngular Project Part 4

For this assignment, follow the instructions in the Lecture 26 Markdown file.

1. Configure your routes (`/products`, `/products/:id`, `/cart`).
2. Add a `<router-outlet>`.
3. Enable `withComponentInputBinding()` to easily read the `:id` parameter!

Happy coding!
