# DataForge Project Part 3

For this assignment, follow the instructions in the Lecture 21 Markdown file.

1. Implement a `@Required` field decorator using standard TC39 decorators and auto-accessors (`accessor`).
2. Apply the decorator to the `User` class to ensure `name` is never set to an empty string.

Happy coding!
