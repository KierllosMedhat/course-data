# FinanceTracker Project Part 5

For this assignment, follow the instructions in the Lecture 37 Markdown file.

1. Install EF Core Sqlite and Design packages.
2. Create a `FinanceContext` inheriting from `DbContext`.
3. Replace your JSON file logic with `Transactions.Add()`, `ToListAsync()`, and `SaveChangesAsync()`.

Happy coding!
