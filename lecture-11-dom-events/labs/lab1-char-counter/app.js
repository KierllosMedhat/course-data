const input = document.querySelector("#myInput");
const counter = document.querySelector("#counter");

// TODO: Add an 'input' event listener to the textarea.
// Every time the user types, update the counter text to show currentLength / 200.
// Example: "15 / 200"
