const cart = document.querySelector("#cart");

// TODO: Add a single 'click' event listener to the #cart container (Event Delegation!)
// Use event.target.closest('.delete-btn') to check if a delete button was clicked.
// If it was, find the parent '.cart-item' and remove() it from the DOM.
