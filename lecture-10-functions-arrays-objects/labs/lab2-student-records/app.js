const students = [
    { id: 1, name: "Alice", grade: "A" },
    { id: 2, name: "Bob", grade: "B" },
    { id: 3, name: "Charlie", grade: "A" }
];

// TODO: Complete this function. Use parameter destructuring to grab 'name' and 'grade' directly!
function printStudent(student) {
    // console.log `${name} got a ${grade}!`
}

// TODO: Use forEach to loop over 'students' and pass each to printStudent
